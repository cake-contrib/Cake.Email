mode: ContinuousDelivery
branches:
  release:
    regex: releases?[/-]
    mode: ContinuousDeployment
    tag: rc
  develop:
    regex: dev(elop)?(ment)?$
    mode: ContinuousDeployment
    tag: alpha
  hotfix:
    regex: hotfix(es)?[/-]
    mode: ContinuousDeployment
    tag: beta
ignore:
  sha: []
